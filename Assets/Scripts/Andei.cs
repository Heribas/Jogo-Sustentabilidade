using UnityEngine;

public class Andei : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("ababababa");
    }

    // Update is called once per frame
    void Update()
    {
    
    }
}
